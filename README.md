# システムの導入

1. Gitからクローンする。

1. 設定ファイルの作成<br>
`$ cp .env.local.example .env.local`

1. Node.jsのパッケージをインストールする。<br>
`$ npm install`

# サービスの起動

Next.jsのサーバーを起動する。<br>
`npm run dev`

掲示板にアクセスする。`http://localhost:3000/board`。
